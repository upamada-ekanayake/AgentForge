import { RoutePlaceholder } from "@/components/layout/route-placeholder";

export default function AuthPage() {
  return (
    <RoutePlaceholder
      title="Auth"
      description="Register and login screens will be added when the auth backend module is implemented."
    />
  );
}
