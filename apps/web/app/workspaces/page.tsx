import { RoutePlaceholder } from "@/components/layout/route-placeholder";

export default function WorkspacesPage() {
  return (
    <RoutePlaceholder
      title="Workspaces"
      description="Workspace creation and member management will be added after the database foundation is ready."
    />
  );
}
